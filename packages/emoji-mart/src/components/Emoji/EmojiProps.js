import PickerProps from '../Picker/PickerProps'

export default {
  fallback: '',
  id: '',
  native: '',
  shortcodes: '',
  size: '',

  // Shared
  set: PickerProps.set,
  skin: PickerProps.skin,
}
